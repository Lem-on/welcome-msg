API.chatLog(/me :white_check_mark: WELCOME BOT RUN );

API.on(API.USER_JOIN, function(data){
  if(message == ""){API.sendChat("@" + data.username Ahoj");
 } else {API.sendChat(message 
 + " @" + data.username);
}

